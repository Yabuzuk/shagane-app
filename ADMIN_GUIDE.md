# Панель администратора

## Как получить доступ к панели администратора:

### 1. Узнайте свой Telegram ID

Есть несколько способов:

**Способ 1: Через бота**
1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Нажмите "Start"
3. Бот отправит вам ваш ID

**Способ 2: Через консоль браузера**
1. Войдите в приложение через Telegram
2. Откройте консоль браузера (F12)
3. Введите: `JSON.parse(localStorage.getItem('telegramUser')).id`
4. Скопируйте полученное число

### 2. Добавьте свой ID в список администраторов

В файле `src/App.jsx` найдите строку:

```javascript
const ADMIN_IDS = [123456789]; // Добавьте свой Telegram ID
```

Замените `123456789` на ваш реальный Telegram ID:

```javascript
const ADMIN_IDS = [987654321]; // ваш ID
```

Можно добавить несколько администраторов:

```javascript
const ADMIN_IDS = [987654321, 123456789, 555555555];
```

### 3. Перезапустите приложение

```bash
npm run dev
```

## Возможности панели администратора:

### ✅ Добавление товаров
- Название
- Цена
- Вес/Количество
- Описание
- Выбор иконки из 12 вариантов

### ❌ Удаление товаров
- Удаление товара из каталога
- Автоматическое удаление из корзин пользователей

### 📋 Просмотр всех товаров
- Список всех товаров с деталями
- Количество товаров в каталоге

## Интерфейс администратора:

- В профиле отображается бейдж "АДМИН"
- В нижней панели появляется кнопка "⚙️ Админ"
- Доступ к панели только у пользователей из списка ADMIN_IDS

## Безопасность:

⚠️ **Важно для продакшена:**
- Проверка прав администратора должна быть на сервере
- Не храните список администраторов в клиентском коде
- Используйте базу данных для управления ролями
- Добавьте логирование действий администратора
